[build-system]
requires = [
    "scikit-build>=0.12",
    "setuptools>=42",
    "setuptools_scm>=5",
    "wheel",
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
build = "cp311-musllinux*"
build-verbosity = 3
repair-wheel-command = "cp {wheel} {dest_dir}/"
test-extras = "test"
test-command = "pytest {project}/tests"
musllinux-x86_64-image = "patchelf_build_x86_64:cibw_local"
musllinux-i686-image = "patchelf_build_i686:cibw_local"
musllinux-aarch64-image = "patchelf_build_aarch64:cibw_local"
musllinux-ppc64le-image = "patchelf_build_ppc64le:cibw_local"
musllinux-s390x-image = "patchelf_build_s390x:cibw_local"

[tool.cibuildwheel.linux.environment]
CXXFLAGS = "-D_FORTIFY_SOURCE=2 -fstack-protector-strong -Wformat -Werror=format-security -O2 -static"
